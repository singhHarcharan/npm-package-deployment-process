npm init -y     --> "initialize node.js project (basically creates a package.json file)" 

npx tsc --init  --> "initialize typscript project in same directory (basically creates a tsconfig.json file)"


npm login

npm publish --access=public --> "Publish npm package to npmjs.org"

npm pack -> "generate a .tgz file where the file are present which has been uploaded on npmjs.org"
